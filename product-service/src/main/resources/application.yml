server:
  port: 8082   # ✅ FIXED

spring:
  application:
    name: product-service

  datasource:
    url: jdbc:h2:mem:productdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update   # ✅ FIXED (NOT create-drop)
    show-sql: true

# ===============================
# EUREKA CONFIG
# ===============================
eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/

  # instance:
  #   prefer-ip-address: true
  #   non-secure-port: 8082   # ✅ VERY IMPORTANT
  instance:
    hostname: product-service
    non-secure-port: 8082
    prefer-ip-address: false
management:
  endpoints:
    web:
      exposure:
        include: health
# server:
#   port: 8081

# spring:
#   application:
#     name: product-service
#   datasource:
#     url: jdbc:h2:mem:productdb
#     driver-class-name: org.h2.Driver
#     username: sa
#     password:
#   h2:
#     console:
#       enabled: true
#   jpa:
#     hibernate:
#       ddl-auto: create-drop
#     show-sql: true

# eureka:
#   client:
#     service-url:
#       #defaultZone: http://localhost:8761/eureka/
#       defaultZone: http://eureka-server:8761/eureka/
